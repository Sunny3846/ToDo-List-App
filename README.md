# 📝 ToDo List Application

A simple and clear ToDo List web application built using **HTML5**, **CSS3**, and **JavaScript DOM manipulation**.  
It allows users to add, mark, and delete tasks easily, while saving data in the browser using **localStorage**.

---

## 🚀 Features

- Add new tasks to the list  
- Mark tasks as complete using a checkbox  
- Delete individual tasks  
- Automatically save and load tasks from localStorage  
- Simple, clean, and responsive design

---

## 🧩 Technologies Used

- **HTML5** – for page structure  
- **CSS3** – for styling and layout  
- **JavaScript (Vanilla JS)** – for DOM manipulation and interactivity  
- **localStorage** – for saving data in the browser

---

## ⚙️ Setup Instructions

To run this project locally:

1. Clone the repository:
   ```bash
   git clone (https://github.com/Sunny3846/ToDo-List-App)
2.Make sure u run this code on gitbash or vscode for best results.
3.codesandbox is not recommended.
